#include "../all.cpp"

class Solution {
public:
    void wiggleSort(vector<int>& nums) {
        int n = nums.size();
        vector<int> ret(n);
        bool find_big = true;
        ret[0]=nums[0];
        for(int i=1;i<n;++i)
        {
            if(find_big)
            {
                
            }
            else
            {

            }
            find_big = !find_big;
        }
    }
};