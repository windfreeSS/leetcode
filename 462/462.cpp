#include "../all.cpp"

// 题目描述，看起来，是和数学相关的。最开始想到的是方差，标准差，期望这些，发现性质不太相符
// 后面，找到了中位数，中位数有这样的性质：对于一个数列，所有数与中位数的绝对差之和最小。
// 这个性质是和题目，一致的。
// 但是，没有找到非常严格的证明，故此题暂时不做。

class Solution {
public:
    int minMoves2(vector<int>& nums) {
        return 0;
    }
};